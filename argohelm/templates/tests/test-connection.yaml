apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "argohelm.fullname" . }}-test-connection"
  labels:
    {{- include "argohelm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "argohelm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
